spring:
  application:
    name: first-project


#API KEY OPEN-AI
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4.1
          temperature: 0.2

    ollama:
      base-url: http://localhost:11434/
      chat:
        options:
          model: codellama:latest
          temperature: 0.7