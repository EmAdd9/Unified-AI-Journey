spring:
  profiles:
    active: ${env}
  application:
    name: first-project
  #API KEY OPEN-AI
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4.1
          temperature: 0.2